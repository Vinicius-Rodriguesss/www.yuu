# URL do backend (backend/server.ts). Em device físico use o IP da sua máquina
# na rede local, ex.: http://192.168.0.10:3000 — "localhost" aponta pro próprio
# celular. Lido em src/lib/config.ts via expo-constants / process.env.
EXPO_PUBLIC_API_URL=http://localhost:3000
